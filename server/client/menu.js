const trigger = require('smartux-connect').trigger;
trigger({
    id: 'transform.changepassword.undefined.dialog_confirm',
    events: ['transform.changepassword.undefined.dialog_confirm']
}, async (session, models, vars) => {
});